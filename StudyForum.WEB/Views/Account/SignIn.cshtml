@{
    Layout = "~/Views/Shared/_LayoutMin.cshtml";
    ViewBag.Title = "SignIn";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>SignIn</title>
    <link rel="stylesheet" href="~/Content/bootstrap.css"/>
    <link rel="stylesheet" href="~/Content/Account.css" />
</head>
<body>

<div class="signin-contaner col-md-12 col-md-offset-4">
    <h2>Войти в систему</h2>

    @using (Html.BeginForm("SignIn", "Account", FormMethod.Post))
    {
        <div class="form-group">
            @Html.Label("Адрес электронной почты", new {@class = "control-label"})
            @Html.TextBox("Email", null, new {@class = "form-control"})
        </div>

        <div class="form-group">
            @Html.Label("Пароль", new {@class = "control-label"})
            @Html.Password("Password", null, new {@class = "form-control"})
        </div>

        <div class="form-group">
            @Html.Label("Повтор пароля", new { @class = "control-label" })
            @Html.Password("RepeatPassword", null, new { @class = "form-control" })
        </div>

        <div class="form-group">
            @Html.CheckBox("RememberMe", null)
            @Html.Label("Запомнить меня")
        </div>

        <input type="submit" class="btn btn-default" value="Войти"/>
    }
    
    <div class="row">
        <div class="col-md-12">
            <a href="">Проблемы с входом?</a>
        </div>
        
        <div class="col-md-12">
            <a href="">Зарегистрироваться</a>
        </div>
    </div> 
</div>
</body>
</html>
